{% extends 'base.html.twig' %}

{% block title %}{{ 'tag.edit'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'tag.edit'|trans }}</h1>

    {{ include('tag/_form.html.twig', {'button_label': 'Zapisz'}) }}

<a href="{{ path('app_tag_index') }}" class="btn btn-secondary mb-2">
    {{ 'action.back_to_list'|trans }}
</a>

<a href="{{ path('app_tag_delete_confirm', {'id': tag.id}) }}" class="btn btn-danger">
    Usuń
</a>
{% endblock %}
