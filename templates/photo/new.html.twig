{% extends 'base.html.twig' %}

{% block title %}New Photo{% endblock %}

{% block body %}
    <h1>{{ 'photo.new'|trans }}</h1>

    {{ include('photo/_form.html.twig') }}

    <a href="{{ path('app_photo_index') }}">{{ 'action.back_to_list'|trans }}</a>
{% endblock %}
