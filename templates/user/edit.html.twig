{% extends 'base.html.twig' %}

{% block title %}{{ 'user.edit'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'user.edit'|trans }}</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Zapisz'}) }}

    <p>
        <a href="{{ path('app_user_password', {'id': user.id}) }}" class="btn btn-secondary">
            Zmień hasło
        </a>
    </p>

<a href="{{ path('app_user_index') }}" class="btn btn-secondary mb-2">
    {{ 'action.back_to_list'|trans }}
</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
