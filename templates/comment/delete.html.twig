{% extends 'base.html.twig' %}

{% block title %}Usuwanie komentarza{% endblock %}

{% block body %}
    <h1>Usuwanie komentarza</h1>

    <p>
        Czy na pewno chcesz usunąć komentarz użytkownika "{{ comment.nick }}"?
    </p>

    {{ include('comment/_delete_form.html.twig') }}

    <a href="{{ path('app_comment_show', {'id': comment.id}) }}" class="btn btn-secondary">
        Anuluj
    </a>
{% endblock %}
