{% extends 'base.html.twig' %}

{% block title %}{{ 'comment.edit'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'comment.edit'|trans }}</h1>

    {{ include('comment/_form.html.twig', {'button_label': 'Zapisz'}) }}

<a href="{{ path('app_comment_index') }}" class="btn btn-secondary mb-2">
    {{ 'action.back_to_list'|trans }}
</a>

<a href="{{ path('app_comment_delete_confirm', {'id': comment.id}) }}" class="btn btn-danger">
    Usuń
</a>
{% endblock %}
