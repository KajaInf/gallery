{% extends 'base.html.twig' %}

{% block title %}Usuwanie galerii{% endblock %}

{% block body %}
    <h1>Usuwanie galerii</h1>

    <p>
        Czy na pewno chcesz usunąć galerię "{{ gallery.title }}"?
    </p>

    {{ include('gallery/_delete_form.html.twig') }}

    <a href="{{ path('app_gallery_show', {'id': gallery.id}) }}" class="btn btn-secondary">
        Anuluj
    </a>
{% endblock %}
